# SenseBox-Mini
SenseBox Mini is a compact ESP32-based room monitoring device that measures temperature, humidity, and air quality. It hosts a local web interface over WiFi where users can view live sensor data, configure alert thresholds, and control sound notifications — all without relying on cloud services.
